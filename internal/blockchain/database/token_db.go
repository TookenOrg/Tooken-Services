package database

import (
	"context"

	"github.com/TookenOrg/tooken-services/internal/api/server"
)

func GetTokenByName(ctx context.Context, tokenName string) (token *server.TokenInfos, err error) {
	return
}
